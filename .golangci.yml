version: "2"

run:
  timeout: 5m

linters:
  disable-all: true
  enable:
    - errcheck
    - govet
    - gosimple
    - ineffassign
    - misspell

issues:
  exclude-rules:
    - path: ".*_test\\.go$"
      linters:
        - errcheck
